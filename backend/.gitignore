# Dependencies
node_modules/

# Environment variables & secrets
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# (Optional) Keep a template file tracked in Git so team members know what variables are needed
!.env.example
